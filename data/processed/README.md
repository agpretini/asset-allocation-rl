# Processed Data

This directory is organized by artifact generator. Each subdirectory maps to a
script under `scripts/`.

Use `data/README.md` as the source of truth for what each subdirectory contains
and how to regenerate it.
