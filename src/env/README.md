# Action Space

`action_space.py` generates all valid long-only portfolio allocations for a
configurable number of assets and weight increment. It works in integer units
internally so allocations obey the portfolio constraints by construction.
